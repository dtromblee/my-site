/*global angular*/
angular.module('mySite')
    .config(['$resourceProvider', function($resourceProvider) {
        // Uncomment to disable strip trailing slashes from calculated URLs
        // $resourceProvider.defaults.stripTrailingSlashes = false;
    }]);